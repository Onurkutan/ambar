build/
build-*/
cmake-build-*/
*.o
*.a
compile_commands.json
.cache/
.idea/
.vscode/

# A database directory left in the repo root by running a tool (ambar_bench,
# ambar_crash_test) with a relative path instead of a temp one.
CURRENT
LOCK
LOG
MANIFEST-*
*.sst
*.log
*.dbtmp
*.jnl

# What a fuzz run leaves behind: the seed corpora fuzz_seeds writes, and the
# inputs libFuzzer saves when one of them crashes, hangs or leaks.
corpus/
crashes/
crash-*
leak-*
timeout-*
oom-*
